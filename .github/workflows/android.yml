name: Сборка APK

# APK собирается по тегу и попадает в релиз: держать бинарник в репозитории
# смысла нет, а скачивать его людям нужно.
on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write # нужно, чтобы приложить файл к релизу

jobs:
  build:
    runs-on: ubuntu-latest

    # Секреты в `if` у шага использовать нельзя — контекст secrets там
    # недоступен, и workflow падает на разборе. Поэтому раскрываем их
    # в env один раз на уровне job.
    env:
      NEWDAY_KEYSTORE: ${{ github.workspace }}/release.keystore
      NEWDAY_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      NEWDAY_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - run: npm ci

      # Версия приложения — из тега: v1.2.3 → versionName 1.2.3.
      # versionCode должен монотонно расти, иначе Android не даст обновиться,
      # поэтому считаем его из номера версии: 1.2.3 → 10203.
      - name: Определить версию
        run: |
          RAW="${GITHUB_REF_NAME#v}"
          NAME="${RAW:-0.0.0}"
          IFS=. read -r MA MI PA <<< "$NAME"
          CODE=$(( ${MA:-0} * 10000 + ${MI:-0} * 100 + ${PA:-0} ))
          [ "$CODE" -gt 0 ] || CODE=1
          echo "NEWDAY_VERSION_NAME=$NAME" >> "$GITHUB_ENV"
          echo "NEWDAY_VERSION_CODE=$CODE" >> "$GITHUB_ENV"
          echo "версия $NAME (code $CODE)"

      - name: Восстановить ключ подписи
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            echo "$KEYSTORE_BASE64" | base64 -d > "$NEWDAY_KEYSTORE"
            echo "ключ восстановлен"
          else
            # Пусть путь остаётся несуществующим: build.gradle это увидит
            # и подпишет отладочным ключом, а не выдаст неустановимый APK.
            echo "::warning::ANDROID_KEYSTORE_BASE64 не задан, APK будет подписан отладочным ключом"
          fi

      - name: Перенести веб-часть в проект Android
        run: npx cap sync android

      # chmod здесь, а не только в индексе git: проект ведётся с Windows, где
      # бит исполнения не хранится и любой `git add` его сбрасывает. Без этого
      # сборка падает на `./gradlew: Permission denied`.
      - name: Собрать релизный APK
        working-directory: android
        run: |
          chmod +x ./gradlew
          # Две сборки: AAB для Google Play (он APK не принимает вовсе) и APK
          # сборки rustore — им же раздаёт сайт. Разница между ними одна:
          # в APK есть самообновление, в AAB его нет.
          ./gradlew bundlePlayRelease assembleRustoreRelease --no-daemon

      - name: Проверить подписи и назвать файлы по-человечески
        run: |
          BT=$(ls -d "$ANDROID_HOME"/build-tools/* | sort -V | tail -1)
          OUT=android/app/build/outputs
          APK="$OUT/apk/rustore/release/app-rustore-release.apk"
          AAB="$OUT/bundle/playRelease/app-play-release.aab"

          "$BT/apksigner" verify --print-certs "$APK"
          # AAB подписан jar-подписью, apksigner про него не знает
          jarsigner -verify -strict "$AAB" | tail -2

          # «Android Debug» в подписи значит, что ключ не подхватился, а сборка
          # выглядит настоящей. В магазин такая не годится — падаем здесь.
          if "$BT/apksigner" verify --print-certs "$APK" | grep -q "CN=Android Debug"; then
            echo "::error::APK подписан отладочным ключом — проверьте ANDROID_KEYSTORE_BASE64"
            exit 1
          fi

          mkdir -p dist
          cp "$APK" "dist/NewDay-$NEWDAY_VERSION_NAME.apk"
          cp "$AAB" "dist/NewDay-$NEWDAY_VERSION_NAME.aab"
          rm -f "$NEWDAY_KEYSTORE"
          ls -la dist

      # Главный адрес раздачи — сам сайт: приложение обновляется со своего
      # сервера, и человеку не нужен доступ к репозиторию. Выкладываем сразу
      # после сборки, чтобы «новая версия» и «новая версия на сайте» были
      # одним событием.
      - name: Выложить APK на сайт
        env:
          SITE: ${{ secrets.APK_UPLOAD_URL }}
          TOKEN: ${{ secrets.APK_UPLOAD_TOKEN }}
        run: |
          if [ -z "$SITE" ] || [ -z "$TOKEN" ]; then
            echo "::warning::APK_UPLOAD_URL или APK_UPLOAD_TOKEN не заданы — APK останется только в релизе"
            exit 0
          fi
          APK="dist/NewDay-$NEWDAY_VERSION_NAME.apk"
          CODE=$(curl -sS -o /tmp/upload.json -w '%{http_code}' \
            -X POST "$SITE/api/v1/app/upload?versionName=$NEWDAY_VERSION_NAME" \
            -H "X-Upload-Token: $TOKEN" \
            -H "Content-Type: application/octet-stream" \
            --data-binary "@$APK")
          cat /tmp/upload.json; echo
          # 409 — на сайте уже лежит версия не старше этой. Это не авария
          # сборки: перезапуск того же тега не должен ронять workflow.
          if [ "$CODE" = "200" ]; then
            echo "выложено на $SITE"
          elif [ "$CODE" = "409" ]; then
            echo "::warning::на сайте уже версия не старше — оставляем как есть"
          else
            echo "::error::выкладка не удалась, код $CODE"
            exit 1
          fi

      # Релиз на GitHub — альтернатива: удобно, когда репозиторий открыт,
      # но приложение обновляется не отсюда.
      - name: Приложить APK к релизу
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/NewDay-*.apk
            dist/NewDay-*.aab
          body: |
            Приложение обновляется само: при запуске оно спрашивает сервер,
            нет ли версии новее. Этот файл — на случай первой установки
            или установки вручную.

            Установка: разрешите установку из неизвестных источников,
            затем откройте файл. При первом запуске укажите адрес своего
            сервера — по умолчанию приложение смотрит на newday.appswire.ru.

            Будильнику нужны два разрешения, приложение попросит их само:
            точные будильники и показ поверх других окон. Без второго
            экран не включится, если телефон разблокирован.
