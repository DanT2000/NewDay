/**
 * Настройки всего экземпляра, а не пользователя.
 *
 * Нужны для подключения ИИ: модель и ключ задаёт администратор один раз,
 * и ими пользуются все. Хранить их в user_settings нельзя — это настройка
 * сервера, а не человека; в переменных окружения тоже неудобно, потому что
 * тогда для смены ключа нужен доступ к серверу и перезапуск.
 *
 * Ключ храним как есть: это секрет самого владельца экземпляра, и сервер
 * должен уметь им пользоваться. Наружу он никогда не отдаётся — только
 * признак «задан» и последние четыре символа.
 */

module.exports = {
  version: 4,
  name: 'app-settings',
  up(db) {
    db.exec(`
      CREATE TABLE IF NOT EXISTS app_settings (
        key        TEXT PRIMARY KEY,
        value      TEXT,
        updated_at TEXT NOT NULL DEFAULT (datetime('now'))
      );
    `);
  },
};
