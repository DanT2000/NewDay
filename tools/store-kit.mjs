/**
 * Набор для загрузки в магазины: всё по папкам, готовое к отправке.
 *
 *   node tools/store-kit.mjs
 *
 * Собирает store/google-play/ и store/rustore/ — в каждой ровно то, что просит
 * этот магазин, и ничего лишнего. Тексты кладутся отдельными файлами, чтобы
 * копировать их прямо в поля консоли, а не выкусывать из markdown.
 *
 * Почему у магазинов разное число скриншотов: Google Play принимает для
 * телефона от 2 до 8 кадров, RuStore — больше. Поэтому в Play уходят восемь
 * отобранных, в RuStore все десять; какие два не попали в Play и почему —
 * написано в ЧТО-ЗАГРУЖАТЬ.md рядом.
 *
 * Сборки (AAB и APK) копируются, но в git не попадают: это артефакты, они
 * весят по девять мегабайт и пересобираются одной командой.
 */
import { copyFileSync, existsSync, mkdirSync, readdirSync, rmSync, statSync, writeFileSync } from 'node:fs';
import { basename, join } from 'node:path';

const ROOT = 'store';
const SHOTS = join(ROOT, 'screens');
const ID = 'ru.appswire.newday';
const VERSION = '1.0.8';
const CODE = '10008';

const AAB = 'android/app/build/outputs/bundle/release/app-release.aab';
const APK = 'android/app/build/outputs/apk/release/app-release.apk';
const ICON = 'public/icons/icon-512.png';
const FEATURE = join(ROOT, 'feature-1024x500.png');

/*
 * Порядок кадров для магазина — не тот, в котором они снимались.
 *
 * Первый кадр человек видит в списке приложений, второй и третий — при
 * листании; дальше смотрят реже. Поэтому сначала «что это» (день), сразу за
 * ним — то, чем приложение отличается (будильник с задачей), и только потом
 * подробности.
 */
const ДЛЯ_PLAY = [
  '01-день', '09-будильник-задача', '10-будильник-qr', '03-расписание-дня',
  '05-привычки', '02-дела-и-питание', '06-заметки', '08-помощник',
];
// В Play не попали: редактор дела и редактор привычки — они повторяют
// расписание и привычки, а лимит восемь кадров.
const ДЛЯ_RUSTORE = null;   // null — значит все, в порядке съёмки

const ТЕКСТЫ = {
  'название.txt': 'NewDay: будильник и день',
  'краткое-описание.txt': 'Будильник с задачей на пробуждение, расписание дня и привычки',
  'полное-описание.txt': `NewDay — это планировщик дня, трекер привычек и будильник, который действительно будит.

БУДИЛЬНИК, КОТОРЫЙ НЕ ВЫКЛЮЧИТЬ СПРОСОНЬЯ

Обычный будильник гасится одним касанием — и вы засыпаете снова. NewDay просит сделать то, для чего надо проснуться:

• решить пример — сложность выбираете сами;
• ввести код, который вы задали заранее;
• повторить последовательность значков;
• отсканировать QR-код, наклеенный там, куда придётся дойти: на чайнике, в ванной;
• пройти заданное число шагов.

Если задача вдруг невыполнима — нет камеры, не отвечает шагомер, — будильник сам предложит пример вместо неё. Остаться без выхода нельзя.

Будильник звонит в беззвучном режиме и при «Не беспокоить», поднимает экран поверх блокировки и переживает перезагрузку телефона. Громкость можно наращивать постепенно, от тихого до полного, за выбранное время — просыпаться не обязательно от испуга. Сигналы настоящие, а не пищание: рассвет, птицы, мягкое пианино, и можно загрузить свой звук.

ДЕНЬ, КОТОРЫЙ ВИДНО ЦЕЛИКОМ

Расписание идёт одной полосой: дела сидят на ней по времени, вложенные — с отступом. Видно, что сейчас, что дальше и сколько осталось. Дела повторяются как нужно — каждый день, по будням, по выбранным дням недели, — и каждое может напомнить о себе заранее.

ПРИВЫЧКИ И СЕРИИ

Отмечайте привычку одним касанием и смотрите, сколько дней держится серия. Челлендж на 21 или 30 дней помогает довести дело до привычки, а не бросить на третий день.

ЗАМЕТКИ

Быстрая заметка или заметка на конкретную дату — чтобы мысль не потерялась между делами.

ПИТАНИЕ И СПОРТ

Приёмы пищи, калории, вес, подходы в зале — если вам это нужно. Не нужно — раздел не мешает.

ПОМОЩНИК

Если владелец сервера подключил ИИ-помощника, план дня можно наговорить или написать словами: «завтра в 7 зарядка, в 9 созвон, вечером бассейн» — и получить готовое расписание.

РАБОТАЕТ БЕЗ ИНТЕРНЕТА

Приложение открывается и работает без сети: день на месте, будильник звонит, отметки ставятся. Когда сеть появится, всё само уедет на сервер и сойдётся с другими устройствами.

ВАШИ ДАННЫЕ — ВАШИ

Ни рекламы, ни аналитики, ни сторонних трекеров. Данные лежат на сервере, а не в чужом облаке: по умолчанию это newday.appswire.ru, но на экране входа можно указать свой адрес — и тогда всё остаётся у вас. Выгрузить всё в файл можно в любой момент.`,
  'что-нового.txt': `• Задачи пробуждения по QR-коду и по шагам: код можно наклеить там, куда придётся дойти. Нет камеры или шагомера — будильник сам предложит пример.
• Настоящие звуки вместо синтезированных, с прослушиванием до выбора и своими файлами.
• Постепенное нарастание громкости.
• Настройки разделены на отдельные экраны.
• Расписание выглядит одинаково в любой день, вложенность видна отступом.
• Много исправлений: экран не прыгает при переключениях, время вводится с клавиатуры.`,
};

/*
 * Обоснование чувствительных разрешений для модератора RuStore.
 *
 * В консоли это одно поле «Обоснование» на все разрешения сразу, обязательное.
 * Список разрешений магазин берёт из загруженного APK, поэтому текст должен
 * совпадать со сборкой: REQUEST_INSTALL_PACKAGES здесь больше нет — вместе с
 * самообновлением убрано и оно.
 */
const ОБОСНОВАНИЕ = `NewDay — планировщик дня с будильником. Перечисленные разрешения нужны ровно для будильника и двух его задач пробуждения. Ни одно из них не используется в фоне для сбора данных: в приложении нет рекламы, аналитики и сторонних трекеров, и приложение не устанавливает и не скачивает никаких пакетов.

SCHEDULE_EXACT_ALARM
Будильник обязан звонить в заданную минуту. Время задаёт сам человек в приложении. Без точного будильника система откладывает срабатывание на своё усмотрение, и подъём в 6:00 происходит «примерно тогда» — для будильника это отказ функции.

POST_NOTIFICATIONS
Напоминания о делах, которые человек сам поставил в расписании, и уведомление звонящего будильника. Рассылок, новостей и рекламных уведомлений приложение не отправляет.

SYSTEM_ALERT_WINDOW
Только для того, чтобы поднять экран будильника из фона в момент звонка. Без этого разрешения на разблокированном телефоне вместо экрана будильника показывается всплывающая полоска, и будильник не будит. Никаких наложений поверх других приложений в остальное время нет: экран живёт от звонка до выключения будильника и закрывается сам.

CAMERA
Одна из задач пробуждения — отсканировать QR-код, который человек заранее сам наклеил вне спальни (например на чайнике): чтобы выключить будильник, приходится встать и дойти. Камера включается только на экране звонящего будильника и на экране привязки кода, и выключается сразу после. Распознавание идёт на устройстве, кадры не сохраняются и никуда не передаются.

ACTIVITY_RECOGNITION
Вторая задача пробуждения — пройти заданное число шагов. Шаги считает системный шагомер, число шагов остаётся на устройстве и на сервер не уходит.

Камера и шагомер объявлены необязательными (uses-feature required="false"): на телефоне без них соответствующие задачи не предлагаются вовсе, а будильник даёт вместо них пример на счёт. Приложение работает и без интернета.
`;

const кб = п => `${(statSync(п).size / 1024).toFixed(0)} КБ`;
const мб = п => `${(statSync(п).size / 1024 / 1024).toFixed(1)} МБ`;

function проверить(путь, чтоЭто) {
  if (!existsSync(путь)) {
    console.error(`нет ${чтоЭто}: ${путь}`);
    process.exitCode = 1;
    return false;
  }
  return true;
}

if (!проверить(SHOTS, 'каталога со скриншотами')) process.exit(1);
const все = readdirSync(SHOTS).filter(f => /\.jpg$/i.test(f)).sort();
if (все.length !== 10) {
  console.error(`скриншотов ${все.length}, а должно быть 10 — переснимите:`);
  console.error('  node tools/store-shots.mjs && node tools/store-alarm-shots.mjs');
  process.exit(1);
}

/** Собрать папку одного магазина. */
function собрать({ папка, кадры, сборка, картинка, обоснование, заголовок, пояснение }) {
  const dir = join(ROOT, папка);
  rmSync(dir, { recursive: true, force: true });
  mkdirSync(join(dir, 'скриншоты'), { recursive: true });
  mkdirSync(join(dir, 'тексты'), { recursive: true });

  const список = кадры
    ? кадры.map(имя => все.find(f => f.startsWith(имя)) ?? имя)
    : все;
  const строки = [];
  список.forEach((файл, i) => {
    if (!existsSync(join(SHOTS, файл))) {
      console.error(`нет кадра ${файл}`);
      process.exitCode = 1;
      return;
    }
    // Нумеруем заново: магазин выкладывает кадры в порядке загрузки
    const имя = `${String(i + 1).padStart(2, '0')}-${файл.replace(/^\d\d-/, '')}`;
    copyFileSync(join(SHOTS, файл), join(dir, 'скриншоты', имя));
    строки.push(`${имя}  ${кб(join(dir, 'скриншоты', имя))}`);
  });

  for (const [имя, текст] of Object.entries(ТЕКСТЫ)) {
    writeFileSync(join(dir, 'тексты', имя), `${текст}\n`, 'utf8');
  }

  if (existsSync(ICON)) copyFileSync(ICON, join(dir, 'значок-512.png'));
  if (картинка && existsSync(FEATURE)) copyFileSync(FEATURE, join(dir, basename(FEATURE)));
  // Обоснование разрешений — одним полем, как просит консоль RuStore
  if (обоснование) writeFileSync(join(dir, 'обоснование-разрешений.txt'), ОБОСНОВАНИЕ, 'utf8');
  let сборкаИмя = null;
  if (existsSync(сборка)) {
    сборкаИмя = basename(сборка);
    copyFileSync(сборка, join(dir, сборкаИмя));
  } else {
    console.error(`нет сборки ${сборка} — соберите релиз, см. PUBLISHING.md`);
    process.exitCode = 1;
  }

  writeFileSync(join(dir, 'ЧТО-ЗАГРУЖАТЬ.md'),
    `# ${заголовок}\n\n`
    + `Идентификатор приложения: **${ID}**\n`
    + `Версия: **${VERSION}**, versionCode **${CODE}**\n\n`
    + `${пояснение}\n\n`
    + '## Файлы в этой папке\n\n'
    + (сборкаИмя ? `- **${сборкаИмя}** — сама сборка (${мб(join(dir, сборкаИмя))})\n` : '')
    + `- **значок-512.png** — значок приложения, 512×512 (${кб(join(dir, 'значок-512.png'))})\n`
    + (картинка && existsSync(join(dir, basename(FEATURE)))
      ? `- **${basename(FEATURE)}** — графическое изображение карточки, 1024×500 (${кб(join(dir, basename(FEATURE)))})\n`
      : '')
    + `- **скриншоты/** — ${строки.length} шт., 1080×1920, пропорции 9:16\n`
    + `- **тексты/** — название, краткое и полное описание, «что нового»; копируются как есть\n\n`
    + '## Скриншоты по порядку\n\n'
    + строки.map(s => `- ${s}`).join('\n') + '\n\n'
    + 'Порядок здесь — тот, в котором их стоит загрузить: сначала «что это», '
    + 'сразу за ним будильник с задачей (то, чем приложение отличается), потом подробности.\n\n'
    + '## Что заполнить руками\n\n'
    + 'Ответы на все вопросы консоли — категории, возрастной рейтинг, форма о данных, '
    + 'обоснования разрешений — в [STORE-LISTING.md](../../STORE-LISTING.md).\n',
    'utf8');

  console.log(`${папка}: ${строки.length} кадров${сборкаИмя ? `, ${сборкаИмя}` : ''}`);
  return dir;
}

собрать({
  папка: 'google-play',
  кадры: ДЛЯ_PLAY,
  сборка: AAB,
  картинка: true,
  заголовок: 'Google Play — что загружать',
  пояснение:
    'Play принимает **AAB**, а не APK, и для телефона от 2 до 8 скриншотов — '
    + 'поэтому здесь восемь отобранных. Не попали редактор дела и редактор '
    + 'привычки: они повторяют «расписание дня» и «привычки», а мест восемь.\n\n'
    + '**Ключ подписи.** До первого выпуска выберите «использовать свой ключ» и '
    + 'загрузите наш keystore. Оставите ключ Google — приложение из Play не '
    + 'встанет обновлением поверх сборки с сайта. Подробности — пункт 4 в '
    + 'PUBLISHING.md.',
});

собрать({
  папка: 'rustore',
  обоснование: true,
  кадры: ДЛЯ_RUSTORE,
  сборка: APK,
  картинка: false,
  заголовок: 'RuStore — что загружать',
  пояснение:
    'Загружать **APK**, а не AAB. APK доходит до людей тем же файлом и с нашей '
    + 'подписью — поэтому обновляет сборку с сайта, а не встаёт вторым значком. '
    + 'AAB магазин собрал бы в APK сам и подписал своим ключом: появилась бы '
    + 'третья линия обновлений, не совместимая ни с сайтом, ни с Play.\n\n'
    + 'Скриншотов здесь все десять. Графическое изображение 1024×500 RuStore не '
    + 'спрашивает.\n\n'
    + '**Порядок магазинов не важен:** RuStore можно выложить не дожидаясь Play. '
    + 'Магазины друг о друге не знают, имя пакета один у другого не занимает, и '
    + 'выбор ключа в Play от RuStore не зависит.\n\n'
    + '**Верификация разработчика** делается один раз и занимает время — начните заранее.',
});

writeFileSync(join(ROOT, 'README.md'),
  `# ${ID} — материалы для магазинов\n\n`
  + `Версия **${VERSION}**, versionCode **${CODE}**.\n\n`
  + 'Всё готовое разложено по папкам — брать оттуда и загружать:\n\n'
  + '| Куда | Папка | Что внутри |\n|---|---|---|\n'
  + '| Google Play | [google-play/](google-play/) | AAB, значок, изображение 1024×500, 8 скриншотов, тексты |\n'
  + '| RuStore | [rustore/](rustore/) | APK, значок, 10 скриншотов, тексты |\n\n'
  + 'В каждой папке лежит `ЧТО-ЗАГРУЖАТЬ.md` — что именно этот магазин просит и '
  + 'в каком порядке ставить кадры.\n\n'
  + '## Что где ещё\n\n'
  + '- [STORE-LISTING.md](../STORE-LISTING.md) — ответы на все вопросы консоли: '
  + 'категории, возрастной рейтинг, форма о данных, обоснования разрешений, целевая аудитория.\n'
  + '- [PUBLISHING.md](../PUBLISHING.md) — сборка, подпись, чем отличаются сборки '
  + 'для двух магазинов и проверки перед загрузкой.\n'
  + '- `screens/` — исходные десять кадров, из них и собираются папки магазинов.\n\n'
  + '## Пересобрать\n\n'
  + '```bash\n'
  + 'node tools/store-shots.mjs         # кадры 01–08, веб-часть\n'
  + 'node tools/store-alarm-shots.mjs   # кадры 09–10, будильник на эмуляторе\n'
  + 'node tools/feature-graphic.mjs     # изображение 1024×500\n'
  + 'node tools/store-assets-check.mjs  # размеры, пропорции и вес\n'
  + 'node tools/store-kit.mjs           # разложить по папкам магазинов\n'
  + '```\n\n'
  + 'Сборки (`.aab` и `.apk`) в папках лежат, но в git не хранятся: это артефакты, '
  + 'они пересобираются командами из PUBLISHING.md.\n',
  'utf8');

console.log(`\nГотово. Общий файл: ${join(ROOT, 'README.md')}`);
